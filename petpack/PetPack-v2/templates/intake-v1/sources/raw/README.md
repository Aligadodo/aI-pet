# Raw source holding area

Place original photos or other source media here only in a local intake instance.
Register every file in `sources.manifest.json` with a stable ID, SHA-256, rights,
quality, and intended-use fields before it can be approved.

Raw sources are private authoring inputs. They must not be committed, copied into
`packs/<pack-id>`, or included in `.petpack` archives.
